import React from 'react'
import RouterComp from 'src/page/babylon/RouterComp'
import {RouterCompProps} from 'src/types'

const App = (props: RouterCompProps) => <RouterComp {...props} />

export default App
